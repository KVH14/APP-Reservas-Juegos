package com.reservas.juegos.repository;

// Legacy placeholder removed. Use ReservaRepository instead.
// This file intentionally left as a non-bean placeholder to avoid naming collision.

/*
import com.reservas.juegos.entities.Reserva;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.List;

@Repository
public interface ReservaService extends JpaRepository<Reserva, Long> {
    List<Reserva> findByEmailCliente(String emailCliente);
    List<Reserva> findByProductoId(Long productoId);
    List<Reserva> findByEstado(String estado);
}
*/
